const ENV = process.env;

const ADMIN_NAME = ENV.ADMIN_NAME;
const ADMIN_PASSWORD = ENV.ADMIN_PASSWORD;

const DEVELOPMENT = ENV.DEVELOPMENT === 'true';

HEROKU_DOMAIN = ENV.HEROKU_DOMAIN;
HEROKU_HTTPS = ENV.HEROKU_HTTPS === 'true';

const PLIVO_ID = ENV.PLIVO_ID;
const PLIVO_TOKEN = ENV.PLIVO_TOKEN;
const PLIVO_NUMBER = ENV.PLIVO_NUMBER;

const PORT = ENV.PORT;

const REDIS_URL = ENV.REDIS_URL;

module.exports = {
  ADMIN_NAME,
  ADMIN_PASSWORD,
  DEVELOPMENT,
  PLIVO_ID,
  PLIVO_TOKEN,
  PLIVO_NUMBER,
  PORT,
  REDIS_URL
};
